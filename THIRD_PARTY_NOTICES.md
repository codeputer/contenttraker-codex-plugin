# Third-party notices

The built adapter includes or depends on the following open-source packages. Their upstream licence files remain authoritative.

- `@modelcontextprotocol/sdk` — MIT
- `zod` — MIT
- `@napi-rs/keyring` and its platform packages — MIT
- `esbuild` — MIT (build only)
- `typescript` — Apache-2.0 (build only)

No private ContentTraker application assembly or application source is included.
