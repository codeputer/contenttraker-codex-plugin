# Third-party notices

The built adapter includes code from the following open-source packages. Exact upstream licence texts are included in `THIRD_PARTY_LICENSES`.

- `@modelcontextprotocol/sdk` — MIT
- `ajv` — MIT
- `ajv-formats` — MIT
- `fast-deep-equal` — MIT
- `fast-uri` — BSD-3-Clause
- `json-schema-traverse` — MIT
- `zod` — MIT
- `zod-to-json-schema` — ISC

The following packages are used only to build, type-check, or test the adapter and are not included in the released runtime bundle:

- `@types/node` — MIT
- `esbuild` — MIT
- `selfsigned` — MIT
- `typescript` — Apache-2.0

No private ContentTraker application assembly or application source is included.
