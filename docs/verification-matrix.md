# Cross-platform verification matrix

Evidence snapshot: 2026-07-26. The `0.5.0` account-aware local stdio release implements Epic [#19](https://github.com/codeputer/contenttraker-codex-plugin/issues/19). PR [#20](https://github.com/codeputer/contenttraker-codex-plugin/pull/20) merged to `development` as `86d5157`; its [pull-request matrix](https://github.com/codeputer/contenttraker-codex-plugin/actions/runs/30189461790) and the [post-merge matrix](https://github.com/codeputer/contenttraker-codex-plugin/actions/runs/30218475952) passed on Windows, macOS, Ubuntu, and the Linux container. The full local suite, isolated real-Codex installation/new-task discovery, native Windows Credential Manager round trip, deterministic artifact check, server-instruction and output-schema checks, exact ContentKeeper routing tests, representative reads/writes, measured stdio performance budgets, effective-user restoration, and live direct-storage acceptance pass.

| Requirement | Current evidence | Status |
| --- | --- | --- |
| Portable production bundle | `0.5.0` deterministic SHA256 `3C3E83D851CE13B47461A94F1A19B42CDA50B5C950EBEED3150D2168AA74834D`; a clean extracted marketplace artifact with no `node_modules` starts through the exact `.mcp.json` command, returns server instructions, and exposes 27 tools with output schemas | Verified locally and in the green PR/post-merge matrices |
| Codex plugin discovery | An isolated marketplace installation registered `contenttraker@contenttraker` 0.5.0. The pinned Codex app server launched the installed stdio adapter in a new ephemeral task, retained all 27 output schemas, called `inspect_runtime_capabilities`, and proved the named profile plus required-email policy survived the environment allowlist. A fresh Desktop task reported `ready`, plugin 0.5.0, `contenttraker-codex-adapter`, local `stdio`, valid staging, and no diagnostics | Verified locally, in CI, and in a fresh Codex Desktop task |
| Stdio selection and performance | `initialize` returns model-facing instructions whose first 512 characters state when to use ContentTraker and require exact authorized ContentKeeper resolution. Nine fresh-process Windows samples measured 342.30 ms p95 through `tools/list` and 344.08 ms p95 through the first local tool call, below 2.5 s and 3 s budgets | Verified locally and across the release matrix |
| Local versus connector interface identity | Offline diagnostics report `contenttraker@contenttraker` 0.5.0, `contenttraker-codex-adapter`, local stdio, HTTPS JSON API upstream, and `bundlesRemoteAppMapping: false`. The packaged skill contract rejects substitution of the separate app/connector or remote-first registration | Adapter identity verified by automated fixture and installed-plugin smoke (#19) |
| Windows desktop credential provider | Native Windows Credential Manager set/get/delete round trip passed; the randomized temporary entry was deleted | Verified locally |
| Windows Node | The 0.5.0 artifact, isolated Codex app-server launch, environment-forwarding regression, stdio performance budget, and native Windows Credential Manager round trip passed locally on Node v24.15.0; the randomized credential entry was deleted | Verified locally and on Node 18 release CI |
| macOS desktop and Keychain | The 0.5.0 artifact, isolated Codex install, and native Keychain round trip passed on `macos-latest` | Verified in PR and post-merge release CI |
| Linux desktop | The 0.5.0 artifact and isolated Codex install passed on Ubuntu; WSLg Firefox and GNOME Keyring passed live; no separate non-WSL desktop Secret Service evidence has been captured | CI verified; non-WSL desktop keyring evidence remains partial |
| WSL / Linux Node 18 bundle | The committed bundle started under an isolated WSL Node `v18.19.1` host and exposed the expected tools | Verified locally |
| Current isolated WSL capabilities | Ubuntu 24.04 has D-Bus, WSLg display, `secret-tool`, and GNOME Keyring; a randomized cross-process Secret Service set/get/delete round trip passed and removed its temporary entry; the plugin reports persistent `linux-secret-service` and cross-task restoration available | Keyring verified locally (#11) |
| WSL-native delegated OAuth | Firefox `152.0.6-1` runs inside WSLg; the plugin selected `wsl-desktop` + `wsl-native`, launched PKCE authorization, and completed staging sign-in without Windows browser interoperability | Verified live (#11) |
| Device authorization provider | Advertised/absent/malformed metadata, pending, slow-down, denial, expiry, cancellation, timeout, scope/resource binding, redaction, and delegated keyring persistence tests pass | Plugin side verified; server blocked (#12) |
| Headless manual loopback | Manual URL, callback lifetime, cancellation, timeout, and redaction tests pass | Automated fixture verified |
| Docker/Kubernetes/CI workload OAuth | Consolidated Linux-container workflow passed the fail-closed runtime and portable artifact checks; auto-selection rejects raw bearer configuration, while live metadata advertises no workload grant | Artifact/runtime verified; workload contract/provider blocked (#13) |
| Explicit ephemeral container | Delegated + memory selection and non-restoration behavior are tested | Automated fixture verified |
| Cross-task restoration | Durable identity-policy binding, refresh rotation, new-task rediscovery, stale cleanup, and cross-process lock tests pass; a new WSL adapter process restored/refreshed a test profile without browser interaction | Verified live and by fixtures (#11) |
| Concurrent users and refreshes | Session isolation, caller verification, single-flight refresh, rotation races, revocation, audience binding, and redaction tests pass | Automated fixture verified |
| Production OAuth metadata | Both public documents return HTTP 200 and match resource, authority, endpoints, scopes, bearer headers, code response, PKCE, and public-client auth; authorization-server grants contain only `authorization_code` and omit required `refresh_token` | Blocked at production authorization server (#10) |
| Effective identity policy | Automated tests prove issuer, audience, expiry, required scopes, Codex client claims, token issuer + subject continuity, `/me` subject verification, and optional required-email policy. Security diagnostics expose the durable issuer + subject account key without tokens | Automated fixtures and live restored Phoenix identity verified |
| Authorized ContentKeeper isolation | Every explicit, marker, and exact-registry target is matched against the effective account's `/workspaces` response before a business operation. Reads and writes fail closed on stale or unauthorized targets | Automated fixtures and live authorized ContentKeeper verification pass |
| ContentKeeper precedence and aliases | Neutral fixtures verify canonical `contentKeeperId`, equal-value `workspaceId` compatibility, alias mismatch rejection, preferred discriminated context, marker v1 migration, registry v1/v2 migration, exact repository mapping, project-name non-routing, and environment-default rejection | Automated fixture verified (#19) |
| Live API readiness | `/me`, authorized workspace matching, and the optional workspace-project endpoint returned HTTP 200 after removing the false local-to-remote project fallback | Verified live (#11) |
| Approval-gated asset workflow | Structured contract and mock API tests pass for representative read, search, type listing, update, upload, idempotent create, and lifecycle operations. A fresh Codex Desktop task selected the installed 0.5.0 local adapter for an approved staging Markdown save; draft version 1 was then read back through the exact authorized ContentKeeper. The private asset URI is intentionally not committed to this public repository | Automated fixtures and live direct-storage acceptance verified (#19) |

The `0.5.0` host/container release matrix, review, merge, fresh-Desktop runtime check, restored Phoenix identity, exact ContentKeeper authorization, model selection, approved draft write, and authenticated read-back are complete. The immutable `v0.5.0` tag is the reviewed distribution boundary. After installing or upgrading, fully restart Codex Desktop and open a fresh task; existing tasks can retain an older plugin snapshot, tool catalog, or closed adapter transport.
