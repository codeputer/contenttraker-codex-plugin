# Cross-platform verification matrix

Evidence snapshot: 2026-07-25. The `0.4.1` identity-boundary candidate is tracked in issue [#17](https://github.com/codeputer/contenttraker-codex-plugin/issues/17). Its full local suite, native Windows Credential Manager round trip, deterministic artifact check, and real Codex app-server launch smoke pass. The preceding `0.4.0` release passed both the [push matrix](https://github.com/codeputer/contenttraker-codex-plugin/actions/runs/30161461027) and the [pull-request matrix](https://github.com/codeputer/contenttraker-codex-plugin/actions/runs/30161462130) on Windows, macOS, Ubuntu, and the Linux container; `0.4.1` CI and immutable-tag evidence remain pending until publication.

| Requirement | Current evidence | Status |
| --- | --- | --- |
| Portable production bundle | `0.4.1` deterministic SHA256 `33B0B5030E78B212C5AB4B35B3A42CE21B59F507FB8E482B180468F018029ED9`; a clean extracted marketplace artifact with no `node_modules` starts through the exact `.mcp.json` command and exposes all 27 tools; the actual bundle dependency graph is audited | Verified locally; release CI pending (#17) |
| Codex plugin discovery | An isolated marketplace installation registered `contenttraker@contenttraker` 0.4.1. The pinned Codex app server launched the installed stdio adapter, enumerated all 27 tools, called `inspect_runtime_capabilities`, and proved the host-owned named profile plus required-email policy survived Codex's environment allowlist without exposing the email | Verified locally through Codex; immutable-tag reinstall pending (#17) |
| Local versus connector interface identity | Offline diagnostics report `contenttraker@contenttraker` 0.4.1, `contenttraker-codex-adapter`, local stdio, HTTPS JSON API upstream, and `bundlesRemoteAppMapping: false`. The packaged skill contract directs a task to stop when only the separately exposed connector surface is available; live model-routing verification remains distinct | Adapter identity verified by automated fixture and installed-plugin smoke; skill routing is a live-task check (#17) |
| Windows desktop credential provider | Native Windows Credential Manager set/get/delete round trip passed; the randomized temporary entry was deleted | Verified locally |
| Windows Node 18 | The 0.4.1 artifact, isolated Codex app-server launch, environment-forwarding regression, and native Windows Credential Manager round trip passed locally; the randomized credential entry was deleted | Verified locally; release CI pending (#17) |
| macOS desktop and Keychain | The 0.4.0 artifact, isolated Codex install, and native Keychain round trip passed on `macos-latest` | Verified in release CI (#16) |
| Linux desktop | The 0.4.0 artifact and isolated Codex install passed on Ubuntu; WSLg Firefox and GNOME Keyring passed live; no separate non-WSL desktop Secret Service evidence has been captured | CI verified; non-WSL desktop keyring evidence remains partial |
| WSL / Linux Node 18 bundle | The committed bundle started under an isolated WSL Node `v18.19.1` host and exposed the expected tools | Verified locally |
| Current isolated WSL capabilities | Ubuntu 24.04 has D-Bus, WSLg display, `secret-tool`, and GNOME Keyring; a randomized cross-process Secret Service set/get/delete round trip passed and removed its temporary entry; the plugin reports persistent `linux-secret-service` and cross-task restoration available | Keyring verified locally (#11) |
| WSL-native delegated OAuth | Firefox `152.0.6-1` runs inside WSLg; the plugin selected `wsl-desktop` + `wsl-native`, launched PKCE authorization, and completed staging sign-in without Windows browser interoperability | Verified live (#11) |
| Device authorization provider | Advertised/absent/malformed metadata, pending, slow-down, denial, expiry, cancellation, timeout, scope/resource binding, redaction, and delegated keyring persistence tests pass | Plugin side verified; server blocked (#12) |
| Headless manual loopback | Manual URL, callback lifetime, cancellation, timeout, and redaction tests pass | Automated fixture verified |
| Docker/Kubernetes/CI workload OAuth | Consolidated Linux-container workflow passed the fail-closed runtime and portable artifact checks; auto-selection rejects raw bearer configuration, while live metadata advertises no workload grant | Artifact/runtime verified; workload contract/provider blocked (#13) |
| Explicit ephemeral container | Delegated + memory selection and non-restoration behavior are tested | Automated fixture verified |
| Cross-task restoration | Durable identity-policy binding, refresh rotation, new-task rediscovery, stale cleanup, and cross-process lock tests pass; a new WSL adapter process restored/refreshed a test profile without browser interaction | Verified live and by fixtures (#11) |
| Concurrent users and refreshes | Session isolation, caller verification, single-flight refresh, rotation races, revocation, audience binding, and redaction tests pass | Automated fixture verified |
| Production OAuth metadata | Both public documents return HTTP 200 and match resource, authority, endpoints, scopes, bearer headers, code response, PKCE, and public-client auth; authorization-server grants contain only `authorization_code` and omit required `refresh_token` | Blocked at production authorization server (#10) |
| Effective identity policy | Automated tests prove the packaged adapter blocks before credential restoration when the required email or named profile is absent. Historical isolated-WSL `get_current_user` evidence matched its host policy; the refreshed Phoenix Windows install remains to be verified after publication | Automated and isolated-WSL evidence verified; Phoenix refresh pending (#17) |
| Authorized workspace isolation | Live `list_workspaces` returned only the caller's authorized workspace set; customer workspace names are not recorded here | Verified live (#11) |
| Workspace precedence and conflicts | Neutral fixtures verify explicit ID, explicit name, exact project mapping, default selection, `workspace_conflict`, unauthorized workspace rejection, and no write on failed resolution | Automated fixture verified |
| Live API readiness | `/me`, authorized workspace matching, and the optional workspace-project endpoint returned HTTP 200 after removing the false local-to-remote project fallback | Verified live (#11) |
| Approval-gated asset workflow | Contract and mock API tests pass, including idempotent draft creation after identity and exact workspace verification. Historical client-identity verification occurred only inside its isolated WSL host and correctly stopped before an unrelated target; no client credential is permitted as Phoenix Windows evidence | Correct Phoenix identity required for live Windows staging verification (#17) |

The automated host/container release matrix is complete for `0.4.0`; `0.4.1` release CI remains to run. The general release gates are to merge the reviewed commit and publish/install the immutable tag. The separate Phoenix workstation acceptance gate is to disable the ContentTraker.com app/connector in Codex host configuration, restart Codex, and verify the Phoenix Windows identity from a fresh task before NRRCC work. Automated fixtures prove adapter behavior; they do not substitute for native host, authorization-server, identity, workspace, or model-routing evidence.
